# Copy this file to keystore.properties and fill it locally.
# Never commit keystore.properties or the referenced keystore.
storeFile=release-signing.jks
storePassword=change-me
keyAlias=pageloom-release
keyPassword=change-me
