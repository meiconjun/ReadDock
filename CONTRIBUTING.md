# 贡献指南

感谢参与 PageLoom。提交 Issue 或 Pull Request 前，请先确认问题可以在公开仓库中安全复现，且不包含真实商业网站数据、个人数据、Cookie、凭据、私有 URL 或未授权漫画内容。

## 开发流程

1. Fork 仓库并创建短生命周期分支。
2. 使用 Java 17、Android SDK 35 和 Gradle 8.9。
3. 运行相关单元测试和 `:app:assembleDebug`。
4. 如果改动插件运行时，补充离线合成 fixture 测试。
5. 如果改动用户可见文案，确认没有开发实现细节、内部名称或测试内容。
6. 提交前运行 `git diff --check`。

## 数据源插件

主仓库只接受通用插件协议、权限模型、运行时能力和合成 fixture。真实数据源必须放在独立仓库，并由作者证明授权、许可证和目标网站条款合规。任何绕过验证码、付费墙、访问控制或反爬机制的代码都不会被接受。

## Pull Request

请说明变更范围、测试命令、用户可见影响和已知限制。保持提交聚焦，不把签名密钥、构建产物或本机配置加入提交。
